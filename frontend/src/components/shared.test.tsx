import {render,screen} from '@testing-library/react';import {describe,expect,it} from 'vitest';import {DataTable,EmptyState,KpiCard,StatusChip} from './shared';
describe('composants dashboard',()=>{it('affiche une KPI',()=>{render(<KpiCard label="Alertes" value={4}/>);expect(screen.getByText('Alertes')).toBeInTheDocument();expect(screen.getByText('4')).toBeInTheDocument()});it('affiche un statut',()=>{render(<StatusChip value="critical"/>);expect(screen.getByText('critical')).toBeInTheDocument()});it('affiche l’état vide',()=>{render(<EmptyState/>);expect(screen.getByText(/Aucune donnée/)).toBeInTheDocument()});it('affiche une table',()=>{render(<DataTable rows={[{id:1,status:'ready'}]} columns={['id','status']}/>);expect(screen.getByText('ready')).toBeInTheDocument()})});
